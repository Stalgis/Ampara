import React from "react";
import { View, Text } from "react-native";

const Health = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text style={{ fontSize: 24 }}>Health Screen</Text>
    </View>
  );
};

export default Health;
